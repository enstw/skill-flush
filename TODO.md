# TODO

- [ ] Update the GitHub repo description to match the "flush" metaphor. Current text is the generic "End-of-session skill: migrate agent project memory into living repo docs, commit, push, then wipe. AI-agnostic output." Consider tightening to e.g. "Flush the agent's project memory into the repo (commit + push), then wipe. AI-agnostic output." Run: `gh repo edit --description "..."`.
